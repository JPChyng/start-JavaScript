function loan(loanData){
  const captical =loanData.capital,
        rate = loanData.rate,
        period = loadData.period;
  let interest;
  
  loadData.interest = captical * rate * period;
  
  return loadData;
 }
